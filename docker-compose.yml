version: "3.9"
services:
  mongodb:
    image: mongo:7
    container_name: access-guard-mongodb
    restart: unless-stopped
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_DATABASE: access_guard_ai
    volumes:
      - mongodb_data:/data/db

volumes:
  mongodb_data:

